mixin card(title, content)
    .card.blue-grey.darken-1
        .card-content.white-text
            span.card-title= `${title}`
            p= `${content}`
        .card-action
            a(href="#") Action 1